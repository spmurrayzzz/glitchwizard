import load from '../lib/load.js';

var exports = {
  glitch1: load({ name: 'glitch1', w: 32, h: 44 }),
  glitch2: load({ name: 'glitch2', w: 32, h: 44 }),
  wizard1: load({ name: 'wizard1', w: 32, h: 44 }),
  wizard2: load({ name: 'wizard2', w: 32, h: 44 })
};

export default exports;
